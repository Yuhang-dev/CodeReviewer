# 分级审查系统指南 (Tiered Code Review Guidelines)

为了让 AI 更加精准地理解你的修改意图，并在合适的颗粒度下审查代码，我们强化了 Pull Request 的提交机制。

在提交 Pull Request 时，请在 Description（描述正文）中包含以下 `>>>REVIEW_METADATA<<<` 代码块。系统会自动识别您的指令，动态调整审查严苛等级。

## 支持的参数

| 参数 | 必填 | 含义 |
| :--- | :--- | :--- |
| **Tier** | 是 | 指定本次提交的审查登记（Tier-S / Tier-A / Tier-B / Tier-C）|
| **Context** | 否 | 给 AI 的背景介绍，说明为什么要做这次修改 |
| **Focus** | 否 | 强制指定 AI 着重检查的逻辑焦点（如并发锁、鉴权逻辑等）|

## 审查等级定义
- **Tier-S**: 核心架构与安全底层。AI 将开启最严苛模式，全力死抠并发状态、死锁、内存泄漏、防重放越权与注入等致命问题。
- **Tier-A**: 核心业务修改。AI 侧重检查异常边界条件、空指针、重试逻辑、幂等性。
- **Tier-B**: 常规功能迭代与 Bug 修复。（缺省等级）仅侧重排查拼写错误、规范、代码异味。
- **Tier-C**: 文档、配置、微小样式调整。AI 直接跳过审查，自动给出 LGTM👍，节约审查时间。

## 示例模板

请将以下内容粘贴到您的 Pull Request 描述中，并根据需要进行修改。

```text
>>>REVIEW_METADATA<<<
Tier: Tier-A
Context: 修复了用户短时间内多次点击提交导致的支付渠道回调脏数据 Bug。
Focus: 请务必帮我确认一下增加的分布式乐观锁释放逻辑有没有死锁的风险！
>>>END<<<
```
